{
  "name": "durionbot-worker",
  "version": "1.0.0",
  "description": "LINE webhook handler for DurionBot — Cloudflare Worker",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:list": "wrangler kv:key list --binding DURIONBOT_KV"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
